import { Time } from '@angular/common';

export interface Sale {
    company : string,
    deadline : number,
    subscribers : number,
    value : number,
    cost : number,
    description : string,
    id : number,
    image : string,
    user : number,
}